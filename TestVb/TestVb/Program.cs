using System;

namespace TestVb
{
    public class Program
    {
        public static void Main(string[] args)
        {/*/
              // NULLABLE VALUE Eg
         /*
             int? x = 5;
            int y = 3;
            x = null;
            int newX = x ?? 0; //if x is null,assign 0 to newX,otherwise assign x to newX.
            Console.WriteLine(newX);
            float number = (float) 4.15; //Explicit casting
            float value = 7; //implicit casting
            float solution;

            // PARSING
            string inputValue = "42";

            //int V1 = int.Parse(inputValue);
            // TRY PARSING
            int V1;
            if (int.TryParse(inputValue, out V1))
                Console.WriteLine(V1);
            else
                Console.WriteLine("Error");
            Console.WriteLine(V1);

            int solution1;
           // solution1 = (int) (number * x);
           // solution = number * x;

           // Console.WriteLine(solution);
            Point p1 = new Point();
            Console.WriteLine(p1);
            Console.ReadLine();
        }
        public class Point
        {
            public int x;
            public int y;
            
            public Point()
            {
                x = 0;
                y = 0;
            }
        }
    }*/
}
